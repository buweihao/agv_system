<UserControl x:Class="AgvDispatcher.Modules.ChargeModule.Views.ChargeLeftView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="350"
             Background="#0A111E">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
        </Style>
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#00BFFF"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#0F1A2A"/>
            <Setter Property="BorderBrush" Value="#1F3A60"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="25*"/>
            <RowDefinition Height="25*"/>
            <RowDefinition Height="25*"/>
            <RowDefinition Height="25*"/>
        </Grid.RowDefinitions>

        <!-- 1. 充电概览 -->
        <Border Grid.Row="0" Style="{StaticResource CardBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="▶ 充电概览" Style="{StaticResource SectionTitleStyle}"/>
                <UniformGrid Grid.Row="1" Columns="2" Rows="2" VerticalAlignment="Center">
                    <StackPanel Margin="5" HorizontalAlignment="Center">
                        <TextBlock Text="充电中" Foreground="Gray" FontSize="12" HorizontalAlignment="Center"/>
                        <TextBlock Text="5" Foreground="#00FF7F" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Margin="5" HorizontalAlignment="Center">
                        <TextBlock Text="等待充电" Foreground="Gray" FontSize="12" HorizontalAlignment="Center"/>
                        <TextBlock Text="4" Foreground="#FFD700" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Margin="5" HorizontalAlignment="Center">
                        <TextBlock Text="空闲桩位" Foreground="Gray" FontSize="12" HorizontalAlignment="Center"/>
                        <TextBlock Text="10" Foreground="#00BFFF" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Margin="5" HorizontalAlignment="Center">
                        <TextBlock Text="故障桩位" Foreground="Gray" FontSize="12" HorizontalAlignment="Center"/>
                        <TextBlock Text="1" Foreground="#FF4500" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </StackPanel>
                </UniformGrid>
            </Grid>
        </Border>

        <!-- 2. 电量分布 -->
        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="▶ 电量分布" Style="{StaticResource SectionTitleStyle}"/>
                <Viewbox Grid.Row="1" StretchDirection="DownOnly" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Grid Height="120" Width="240">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Grid Grid.Column="0">
                            <Ellipse Width="100" Height="100" Stroke="#1F3A60" StrokeThickness="10" />
                            <Ellipse Width="100" Height="100" Stroke="#00BFFF" StrokeThickness="10" StrokeDashArray="3 1" />
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="20" FontSize="22" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                <TextBlock Text="总数" FontSize="12" Foreground="Gray" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Grid>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                            <Grid Margin="0,3">
                                <TextBlock Text="≥90%" Foreground="White" FontSize="12" HorizontalAlignment="Left"/>
                                <TextBlock Text="20%" Foreground="#00FF7F" FontSize="12" HorizontalAlignment="Right" FontWeight="Bold"/>
                            </Grid>
                            <Grid Margin="0,3">
                                <TextBlock Text="60%-90%" Foreground="White" FontSize="12" HorizontalAlignment="Left"/>
                                <TextBlock Text="30%" Foreground="#00BFFF" FontSize="12" HorizontalAlignment="Right" FontWeight="Bold"/>
                            </Grid>
                            <Grid Margin="0,3">
                                <TextBlock Text="30%-60%" Foreground="White" FontSize="12" HorizontalAlignment="Left"/>
                                <TextBlock Text="40%" Foreground="#FFD700" FontSize="12" HorizontalAlignment="Right" FontWeight="Bold"/>
                            </Grid>
                            <Grid Margin="0,3">
                                <TextBlock Text="&lt;30%" Foreground="White" FontSize="12" HorizontalAlignment="Left"/>
                                <TextBlock Text="10%" Foreground="#FF4500" FontSize="12" HorizontalAlignment="Right" FontWeight="Bold"/>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Viewbox>
            </Grid>
        </Border>

        <!-- 3. 今日充电统计 -->
        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="▶ 今日充电统计" Style="{StaticResource SectionTitleStyle}"/>
                <UniformGrid Grid.Row="1" Columns="2" Rows="2" VerticalAlignment="Center">
                    <StackPanel Margin="5" HorizontalAlignment="Center">
                        <TextBlock Text="充电次数" Foreground="Gray" FontSize="12" HorizontalAlignment="Center"/>
                        <TextBlock Text="38次" Foreground="White" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Margin="5" HorizontalAlignment="Center">
                        <TextBlock Text="充电电量" Foreground="Gray" FontSize="12" HorizontalAlignment="Center"/>
                        <TextBlock Text="256.8kWh" Foreground="White" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Margin="5" HorizontalAlignment="Center">
                        <TextBlock Text="充电时长" Foreground="Gray" FontSize="12" HorizontalAlignment="Center"/>
                        <TextBlock Text="18.6h" Foreground="White" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Margin="5" HorizontalAlignment="Center">
                        <TextBlock Text="节省成本" Foreground="Gray" FontSize="12" HorizontalAlignment="Center"/>
                        <TextBlock Text="¥256.8" Foreground="#00FF7F" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </StackPanel>
                </UniformGrid>
            </Grid>
        </Border>

        <!-- 4. 充电效率趋势 -->
        <Border Grid.Row="3" Style="{StaticResource CardBorderStyle}" Margin="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <TextBlock Text="▶ 充电效率趋势" Style="{StaticResource SectionTitleStyle}" HorizontalAlignment="Left" Margin="0"/>
                    <TextBlock Text="充电桩利用率 85%" Foreground="#00BFFF" FontSize="12" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </Grid>
                
                <Viewbox Grid.Row="1" Stretch="Fill">
                    <Canvas Width="320" Height="120" Background="Transparent" ClipToBounds="True">
                        <Polyline Points="0,100 40,80 80,90 120,40 160,50 200,20 240,60 280,30 320,10" Stroke="#00BFFF" StrokeThickness="2" />
                        <!-- Area fill simulation -->
                        <Polygon Points="0,120 0,100 40,80 80,90 120,40 160,50 200,20 240,60 280,30 320,10 320,120" Fill="#2000BFFF" />
                    </Canvas>
                </Viewbox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
