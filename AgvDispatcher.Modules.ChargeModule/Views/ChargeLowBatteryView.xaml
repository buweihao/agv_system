<UserControl x:Class="AgvDispatcher.Modules.ChargeModule.Views.ChargeLowBatteryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="#0A111E">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
        </Style>
    </UserControl.Resources>

    <Border Background="#0F1A2A"
            BorderBrush="#1F3A60"
            BorderThickness="1"
            CornerRadius="6"
            Padding="12">
        <StackPanel>
            <TextBlock Text="LOW BATTERY AGV"
                       Foreground="#00BFFF"
                       FontSize="16"
                       FontWeight="Bold"
                       Margin="0,0,0,10"/>

            <TextBlock Text="Waiting for RobotLowBatteryEvent"
                       Foreground="Gray"
                       FontSize="12"
                       Margin="0,0,0,8">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding LowBatteryAgvs.Count}" Value="0">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="195">
                <ItemsControl ItemsSource="{Binding LowBatteryAgvs}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#20FF4500"
                                    BorderBrush="#FF4500"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Padding="8"
                                    Margin="0,0,0,8">
                                <StackPanel>
                                    <Grid Margin="0,0,0,6">
                                        <TextBlock Text="{Binding VehicleId}"
                                                   Foreground="#FF4500"
                                                   FontSize="13"
                                                   FontWeight="Bold"
                                                   HorizontalAlignment="Left"/>
                                        <TextBlock Text="{Binding OccurredAtText}"
                                                   Foreground="Gray"
                                                   FontSize="12"
                                                   HorizontalAlignment="Right"/>
                                    </Grid>
                                    <Grid Margin="0,0,0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding Location}"
                                                   Foreground="White"
                                                   FontSize="12"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding BatteryLevel, StringFormat={}{0:F0}%}"
                                                   Foreground="#FFD700"
                                                   FontSize="12"
                                                   FontWeight="Bold"/>
                                    </Grid>
                                    <Grid>
                                        <TextBlock Text="Recommended station"
                                                   Foreground="Gray"
                                                   FontSize="12"
                                                   HorizontalAlignment="Left"/>
                                        <TextBlock Text="{Binding RecommendedStationText}"
                                                   Foreground="#00BFFF"
                                                   FontSize="12"
                                                   FontWeight="Bold"
                                                   HorizontalAlignment="Right"/>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>
    </Border>
</UserControl>
