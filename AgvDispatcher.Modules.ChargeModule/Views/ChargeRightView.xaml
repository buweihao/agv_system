<UserControl x:Class="AgvDispatcher.Modules.ChargeModule.Views.ChargeRightView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="350"
             Background="#0A111E">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
        </Style>
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#00BFFF"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#0F1A2A"/>
            <Setter Property="BorderBrush" Value="#1F3A60"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource TextBoxBaseStyle}">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="ComboBox" BasedOn="{StaticResource ComboBoxBaseStyle}">
            <Setter Property="Height" Value="30"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="1.8*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

        <!-- 1. 充电桩状态 -->
        <Border Grid.Row="0" Style="{StaticResource CardBorderStyle}">
            <StackPanel>
                <TextBlock Text="▶ 充电桩状态" Style="{StaticResource SectionTitleStyle}"/>
                <Grid Height="120">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Grid Grid.Column="0">
                        <Ellipse Width="100" Height="100" Stroke="#1F3A60" StrokeThickness="10" />
                        <!-- Simulate 92% progress arc by dashing -->
                        <Ellipse Width="100" Height="100" Stroke="#00FF7F" StrokeThickness="10" StrokeDashArray="28 4" />
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="92%" FontSize="22" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                            <TextBlock Text="健康度" FontSize="12" Foreground="Gray" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                        <Grid Margin="0,5">
                            <TextBlock Text="总桩位" Foreground="White" FontSize="14" HorizontalAlignment="Left"/>
                            <TextBlock Text="20" Foreground="White" FontSize="14" HorizontalAlignment="Right" FontWeight="Bold"/>
                        </Grid>
                        <Grid Margin="0,5">
                            <TextBlock Text="正常运行" Foreground="White" FontSize="14" HorizontalAlignment="Left"/>
                            <TextBlock Text="18" Foreground="#00FF7F" FontSize="14" HorizontalAlignment="Right" FontWeight="Bold"/>
                        </Grid>
                        <Grid Margin="0,5">
                            <TextBlock Text="故障" Foreground="White" FontSize="14" HorizontalAlignment="Left"/>
                            <TextBlock Text="1" Foreground="#FF4500" FontSize="14" HorizontalAlignment="Right" FontWeight="Bold"/>
                        </Grid>
                        <Grid Margin="0,5">
                            <TextBlock Text="离线" Foreground="White" FontSize="14" HorizontalAlignment="Left"/>
                            <TextBlock Text="1" Foreground="Gray" FontSize="14" HorizontalAlignment="Right" FontWeight="Bold"/>
                        </Grid>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- 2. 充电告警 -->
        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
            <StackPanel>
                <TextBlock Text="▶ 充电告警" Style="{StaticResource SectionTitleStyle}"/>
                <StackPanel>
                    <Border Background="#20FF4500" BorderBrush="#FF4500" BorderThickness="1" CornerRadius="4" Padding="8" Margin="0,0,0,8">
                        <Grid>
                            <TextBlock Text="AGV-008 激光避障异常" Foreground="#FF4500" FontSize="12" HorizontalAlignment="Left"/>
                            <TextBlock Text="16:02:12" Foreground="Gray" FontSize="12" HorizontalAlignment="Right"/>
                        </Grid>
                    </Border>
                    <Border Background="#20FFD700" BorderBrush="#FFD700" BorderThickness="1" CornerRadius="4" Padding="8" Margin="0,0,0,8">
                        <Grid>
                            <TextBlock Text="充电桩-008 过温保护" Foreground="#FFD700" FontSize="12" HorizontalAlignment="Left"/>
                            <TextBlock Text="15:45:30" Foreground="Gray" FontSize="12" HorizontalAlignment="Right"/>
                        </Grid>
                    </Border>
                    <Border Background="#20FFD700" BorderBrush="#FFD700" BorderThickness="1" CornerRadius="4" Padding="8" Margin="0,0,0,8">
                        <Grid>
                            <TextBlock Text="充电桩-012 网络延迟过高" Foreground="#FFD700" FontSize="12" HorizontalAlignment="Left"/>
                            <TextBlock Text="15:20:05" Foreground="Gray" FontSize="12" HorizontalAlignment="Right"/>
                        </Grid>
                    </Border>
                    <Border Background="#2000BFFF" BorderBrush="#00BFFF" BorderThickness="1" CornerRadius="4" Padding="8" Margin="0">
                        <Grid>
                            <TextBlock Text="系统调度服务已重启" Foreground="#00BFFF" FontSize="12" HorizontalAlignment="Left"/>
                            <TextBlock Text="14:00:00" Foreground="Gray" FontSize="12" HorizontalAlignment="Right"/>
                        </Grid>
                    </Border>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- 3. 充电策略设置 -->
        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}" Margin="0">
            <StackPanel>
                <TextBlock Text="▶ 充电策略设置" Style="{StaticResource SectionTitleStyle}"/>
                
                <Grid Margin="0,5,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="低电量阈值:" Foreground="Gray" VerticalAlignment="Center" Grid.Column="0"/>
                    <TextBox Text="20%" Grid.Column="1"/>
                </Grid>
                
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="充电功率限制:" Foreground="Gray" VerticalAlignment="Center" Grid.Column="0"/>
                    <TextBox Text="2.4kW" Grid.Column="1"/>
                </Grid>

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="充电优先级:" Foreground="Gray" VerticalAlignment="Center" Grid.Column="0"/>
                    <ComboBox Grid.Column="1" SelectedIndex="0">
                        <ComboBoxItem Content="按电量最低优先"/>
                        <ComboBoxItem Content="按排队时间优先"/>
                        <ComboBoxItem Content="均衡分配"/>
                    </ComboBox>
                </Grid>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="夜间充电时段:" Foreground="Gray" VerticalAlignment="Center" Grid.Column="0"/>
                    <TextBox Text="23:00 - 06:00" Grid.Column="1"/>
                </Grid>

                <Button Content="策略保存" Background="#00BFFF" Foreground="White" BorderThickness="0" Height="40" FontSize="16" FontWeight="Bold" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
    </ScrollViewer>
</UserControl>
