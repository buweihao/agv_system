<UserControl x:Class="AgvDispatcher.Modules.DataQueryModule.Views.DataQueryAlarmDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="Transparent">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
        </Style>
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#00BFFF"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style TargetType="ComboBox" BasedOn="{StaticResource ComboBoxBaseStyle}">
            <Setter Property="Height" Value="28"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource TextBoxBaseStyle}">
            <Setter Property="Height" Value="28"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5,0"/>
        </Style>
        <Style TargetType="DatePicker" BasedOn="{StaticResource DatePickerBaseStyle}">
            <Setter Property="Height" Value="28"/>
        </Style>
        
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#112233"/>
            <Setter Property="Foreground" Value="#00BFFF"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#1F3A60"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#1F3A60"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2A3B4C"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 2. 复杂查询表单 -->
        <Border Grid.Row="0" Background="#0A111E" BorderBrush="#1F3A60" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>
                
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="220"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Row 0 -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="查询类型" Foreground="Gray" VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="0" Grid.Column="1" SelectedIndex="0" Margin="0,0,15,10">
                        <ComboBoxItem Content="历史记录"/>
                        <ComboBoxItem Content="实时数据"/>
                    </ComboBox>
                    
                    <TextBlock Grid.Row="0" Grid.Column="2" Text="AGV编号" Foreground="Gray" VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="0" Grid.Column="3" SelectedIndex="0" Margin="0,0,15,10">
                        <ComboBoxItem Content="全部"/>
                        <ComboBoxItem Content="AGV-001"/>
                    </ComboBox>
                    
                    <TextBlock Grid.Row="0" Grid.Column="4" Text="区域" Foreground="Gray" VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="0" Grid.Column="5" SelectedIndex="0" Margin="0,0,15,10">
                        <ComboBoxItem Content="全部区"/>
                        <ComboBoxItem Content="A区"/>
                    </ComboBox>

                    <TextBlock Grid.Row="0" Grid.Column="6" Text="时间范围" Foreground="Gray" VerticalAlignment="Center"/>
                    <StackPanel Grid.Row="0" Grid.Column="7" Orientation="Horizontal" Margin="0,0,0,10">
                        <DatePicker Width="100"/>
                        <TextBlock Text="-" Foreground="Gray" Margin="5,0" VerticalAlignment="Center"/>
                        <DatePicker Width="100"/>
                    </StackPanel>
                    
                    <!-- Row 1 -->
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="任务类型" Foreground="Gray" VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" SelectedIndex="0" Margin="0,0,15,10">
                        <ComboBoxItem Content="全部"/>
                        <ComboBoxItem Content="搬运任务"/>
                    </ComboBox>
                    
                    <TextBlock Grid.Row="1" Grid.Column="2" Text="任务ID" Foreground="Gray" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="1" Grid.Column="3" Text="" Margin="0,0,15,10"/>
                    
                    <TextBlock Grid.Row="1" Grid.Column="4" Text="状态" Foreground="Gray" VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="1" Grid.Column="5" SelectedIndex="0" Margin="0,0,15,10">
                        <ComboBoxItem Content="全部"/>
                        <ComboBoxItem Content="已完成"/>
                        <ComboBoxItem Content="执行中"/>
                    </ComboBox>

                    <!-- Row 2 -->
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="起点" Foreground="Gray" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="" Margin="0,0,15,0"/>
                    
                    <TextBlock Grid.Row="2" Grid.Column="2" Text="终点" Foreground="Gray" VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="2" Grid.Column="3" SelectedIndex="0" Margin="0,0,15,0">
                        <ComboBoxItem Content="全部"/>
                    </ComboBox>
                </Grid>
                
                <StackPanel Grid.Column="1" VerticalAlignment="Bottom">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
                        <Button Content="重置" Background="#334455" Foreground="White" BorderThickness="0" Padding="15,6" Margin="0,0,10,0" Cursor="Hand">
                            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                        </Button>
                        <Button Content="查询" Background="#00BFFF" Foreground="White" BorderThickness="0" Padding="15,6" Cursor="Hand">
                            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                        </Button>
                    </StackPanel>
                    <Button Content="⬇ 导出记录" Background="Transparent" Foreground="#00FF7F" BorderBrush="#00FF7F" BorderThickness="1" Padding="10,4" Cursor="Hand" HorizontalAlignment="Right">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 3. 核心数据表格 -->
        <Border Grid.Row="1" Background="#0A111E" BorderBrush="#1F3A60" BorderThickness="1" CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                    <TextBlock Text="共 120 条" Foreground="Gray" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="10条/页" Foreground="Gray" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <Button Content="&lt;" Background="#112233" Foreground="White" BorderThickness="0" Padding="6,2" Margin="0,0,5,0"/>
                    <Button Content="1" Background="#00BFFF" Foreground="White" BorderThickness="0" Padding="8,2" Margin="0,0,5,0"/>
                    <Button Content="2" Background="#112233" Foreground="White" BorderThickness="0" Padding="8,2" Margin="0,0,5,0"/>
                    <Button Content="3" Background="#112233" Foreground="White" BorderThickness="0" Padding="8,2" Margin="0,0,5,0"/>
                    <Button Content="&gt;" Background="#112233" Foreground="White" BorderThickness="0" Padding="6,2"/>
                </StackPanel>
                
                <DataGrid Grid.Row="1" Margin="10,0,10,10" ItemsSource="{Binding DataList}" AutoGenerateColumns="False" Background="Transparent" 
                          RowBackground="Transparent" AlternatingRowBackground="#0A111E" 
                          Foreground="White" GridLinesVisibility="None" BorderThickness="0" HeadersVisibility="Column"
                          CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="序号" Binding="{Binding Seq}" Width="50"/>
                        <DataGridTextColumn Header="时间" Binding="{Binding Time}" Width="150"/>
                        <DataGridTextColumn Header="AGV编号" Binding="{Binding AgvId}" Width="*"/>
                        <DataGridTextColumn Header="告警级别" Binding="{Binding AlarmLevel}" Width="*"/>
                        <DataGridTextColumn Header="告警代码" Binding="{Binding AlarmCode}" Width="*"/>
                        <DataGridTextColumn Header="告警描述" Binding="{Binding AlarmDesc}" Width="2*"/>
                        <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- 4. 底部汇总信息 -->
        <Border Grid.Row="2" Background="#112233" BorderBrush="#1F3A60" BorderThickness="1" CornerRadius="4" Padding="15,10" Margin="0,15,0,0">
            <StackPanel>
                <TextBlock Text="▶ 汇总信息 (所选时间范围)" Style="{StaticResource SectionTitleStyle}" Margin="0,0,0,10"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="总运行里程" Foreground="Gray" FontSize="11" HorizontalAlignment="Center"/>
                        <TextBlock Text="1286.5 km" Foreground="#00BFFF" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="总运行时长" Foreground="Gray" FontSize="11" HorizontalAlignment="Center"/>
                        <TextBlock Text="87.6 h" Foreground="#00BFFF" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="任务完成数" Foreground="Gray" FontSize="11" HorizontalAlignment="Center"/>
                        <TextBlock Text="256 次" Foreground="#00BFFF" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="3">
                        <TextBlock Text="任务成功率" Foreground="Gray" FontSize="11" HorizontalAlignment="Center"/>
                        <TextBlock Text="98.3 %" Foreground="#00BFFF" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="4">
                        <TextBlock Text="平均速度" Foreground="Gray" FontSize="11" HorizontalAlignment="Center"/>
                        <TextBlock Text="1.36 m/s" Foreground="#00BFFF" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="5">
                        <TextBlock Text="急停次数" Foreground="Gray" FontSize="11" HorizontalAlignment="Center"/>
                        <TextBlock Text="23 次" Foreground="#00BFFF" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="6">
                        <TextBlock Text="平均任务时长" Foreground="Gray" FontSize="11" HorizontalAlignment="Center"/>
                        <TextBlock Text="00:20:35" Foreground="#00BFFF" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="7">
                        <TextBlock Text="人机交互次数" Foreground="Gray" FontSize="11" HorizontalAlignment="Center"/>
                        <TextBlock Text="1286 次" Foreground="#00BFFF" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

