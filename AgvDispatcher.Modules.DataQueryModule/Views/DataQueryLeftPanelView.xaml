<UserControl x:Class="AgvDispatcher.Modules.DataQueryModule.Views.DataQueryLeftPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="220"
             Background="#0A111E">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
        </Style>
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#00BFFF"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#0F1A2A"/>
            <Setter Property="BorderBrush" Value="#1F3A60"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
        <Style TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#112233"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#1F3A60"/>
                                <Setter Property="Foreground" Value="#00BFFF"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border BorderBrush="#1F3A60" BorderThickness="1" CornerRadius="4">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 二级导航 -->
            <StackPanel Grid.Row="0" Margin="0,15,0,20">
                <TextBlock Text="▶ 数据查询" Style="{StaticResource SectionTitleStyle}" Margin="15,0,0,15"/>
                
                <StackPanel Margin="10,0">
                    <RadioButton Content="运行数据" IsChecked="True"/>
                    <RadioButton Content="任务数据"/>
                    <RadioButton Content="充电数据"/>
                    <RadioButton Content="告警数据"/>
                    <RadioButton Content="交互数据"/>
                    <RadioButton Content="能耗数据"/>
                    <RadioButton Content="设备日志"/>
                </StackPanel>
            </StackPanel>

            <!-- 数据概览 -->
            <StackPanel Grid.Row="1" Margin="15,0">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="统计时间:" Foreground="Gray" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox SelectedIndex="0" Background="#0A111E" Foreground="White" BorderBrush="#1F3A60" Width="110">
                        <ComboBoxItem Content="2024-01-12"/>
                        <ComboBoxItem Content="2024-01-11"/>
                    </ComboBox>
                </StackPanel>

                <!-- 指标卡片 -->
                <Border Background="#112233" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                    <Grid>
                        <TextBlock Text="运行里程" Foreground="Gray" FontSize="12" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                        <Viewbox HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,15,0,0" StretchDirection="DownOnly" MaxHeight="22">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="1286.5" Foreground="#00BFFF" FontSize="16" FontWeight="Bold"/>
                                <TextBlock Text=" km" Foreground="Gray" FontSize="10" VerticalAlignment="Bottom" Margin="2,0,0,2"/>
                            </StackPanel>
                        </Viewbox>
                    </Grid>
                </Border>
                
                <Border Background="#112233" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                    <Grid>
                        <TextBlock Text="任务完成数" Foreground="Gray" FontSize="12" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                        <Viewbox HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,15,0,0" StretchDirection="DownOnly" MaxHeight="22">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="256" Foreground="#00FF7F" FontSize="16" FontWeight="Bold"/>
                                <TextBlock Text=" 次" Foreground="Gray" FontSize="10" VerticalAlignment="Bottom" Margin="2,0,0,2"/>
                            </StackPanel>
                        </Viewbox>
                    </Grid>
                </Border>
                
                <Border Background="#112233" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                    <Grid>
                        <TextBlock Text="运行时长" Foreground="Gray" FontSize="12" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                        <Viewbox HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,15,0,0" StretchDirection="DownOnly" MaxHeight="22">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="87.6" Foreground="#9370DB" FontSize="16" FontWeight="Bold"/>
                                <TextBlock Text=" h" Foreground="Gray" FontSize="10" VerticalAlignment="Bottom" Margin="2,0,0,2"/>
                            </StackPanel>
                        </Viewbox>
                    </Grid>
                </Border>
                
                <Border Background="#112233" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                    <Grid>
                        <TextBlock Text="任务成功率" Foreground="Gray" FontSize="12" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                        <Viewbox HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,15,0,0" StretchDirection="DownOnly" MaxHeight="22">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="98.3" Foreground="#00FF7F" FontSize="16" FontWeight="Bold"/>
                                <TextBlock Text=" %" Foreground="Gray" FontSize="10" VerticalAlignment="Bottom" Margin="2,0,0,2"/>
                            </StackPanel>
                        </Viewbox>
                    </Grid>
                </Border>
                
                <Border Background="#112233" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                    <Grid>
                        <TextBlock Text="平均速度" Foreground="Gray" FontSize="12" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                        <Viewbox HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,15,0,0" StretchDirection="DownOnly" MaxHeight="22">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="1.36" Foreground="#FF4500" FontSize="16" FontWeight="Bold"/>
                                <TextBlock Text=" m/s" Foreground="Gray" FontSize="10" VerticalAlignment="Bottom" Margin="2,0,0,2"/>
                            </StackPanel>
                        </Viewbox>
                    </Grid>
                </Border>

                <Border Background="#112233" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                    <Grid>
                        <TextBlock Text="急停次数" Foreground="Gray" FontSize="12" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                        <Viewbox HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,15,0,0" StretchDirection="DownOnly" MaxHeight="22">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="23" Foreground="#00BFFF" FontSize="16" FontWeight="Bold"/>
                                <TextBlock Text=" 次" Foreground="Gray" FontSize="10" VerticalAlignment="Bottom" Margin="2,0,0,2"/>
                            </StackPanel>
                        </Viewbox>
                    </Grid>
                </Border>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
