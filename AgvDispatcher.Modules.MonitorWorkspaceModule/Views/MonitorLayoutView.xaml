<UserControl x:Class="AgvDispatcher.Modules.MonitorWorkspaceModule.Views.MonitorLayoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:local="clr-namespace:AgvDispatcher.Modules.MonitorWorkspaceModule.Views"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" MinHeight="420" />
            <RowDefinition Height="6" />
            <RowDefinition Height="260" MinHeight="220" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#1A2B3C">
            <local:MapView />
        </Grid>

        <GridSplitter Grid.Row="1"
                      Height="6"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Center"
                      Background="#0A111E"
                      BorderBrush="#1F3A60"
                      BorderThickness="0,1"
                      ShowsPreview="True" />

        <Grid Grid.Row="2" Background="#0F1A24">
            <local:RobotListView />
        </Grid>
    </Grid>
</UserControl>
