<UserControl x:Class="AgvDispatcher.Modules.SignalModule.Views.SignalListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="Transparent">

    <UserControl.Resources>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#112233"/>
            <Setter Property="Foreground" Value="#00BFFF"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#1F3A60"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#1F3A60"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2A3B4C"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBox" BasedOn="{StaticResource ComboBoxBaseStyle}">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#1F3A60"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </UserControl.Resources>

    <Border Background="#0F1A2A" BorderBrush="#1F3A60" BorderThickness="1" CornerRadius="6" Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 搜索过滤栏 -->
            <Grid Grid.Row="0" Margin="0,0,0,15">
                <StackPanel Orientation="Horizontal">
                    <ComboBox SelectedIndex="0"><ComboBoxItem Content="全部信号类型"/><ComboBoxItem Content="安全信号"/><ComboBoxItem Content="设备信号"/></ComboBox>
                    <ComboBox SelectedIndex="0"><ComboBoxItem Content="全部状态"/><ComboBoxItem Content="正常"/><ComboBoxItem Content="异常"/><ComboBoxItem Content="离线"/></ComboBox>
                    <TextBox Width="200" Margin="0,0,10,0" Text="🔍 输入信号名称或关联设备"/>
                    
                    <Button Content="搜索" Background="#00BFFF" Foreground="White" BorderThickness="0" Width="60" Height="30" Margin="0,0,10,0" Cursor="Hand">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="2"/></Style></Button.Resources>
                    </Button>
                    <Button Content="重置" Background="#1F3A60" Foreground="White" BorderThickness="0" Width="60" Height="30" Cursor="Hand">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="2"/></Style></Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- 数据表格 -->
            <DataGrid Grid.Row="1" ItemsSource="{Binding SignalList}" AutoGenerateColumns="False" Background="Transparent" 
                      RowBackground="Transparent" AlternatingRowBackground="#0A111E" 
                      Foreground="White" GridLinesVisibility="None" BorderThickness="0" HeadersVisibility="Column"
                      CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="信号ID" Binding="{Binding Id}" Width="*"/>
                    <DataGridTextColumn Header="信号名称" Binding="{Binding Name}" Width="1.5*"/>
                    <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="*"/>
                    
                    <DataGridTemplateColumn Header="状态" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding State}" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="White"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding State}" Value="正常">
                                                    <Setter Property="Foreground" Value="#00FF7F"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding State}" Value="激活">
                                                    <Setter Property="Foreground" Value="#FFD700"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding State}" Value="异常">
                                                    <Setter Property="Foreground" Value="#FF4500"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding State}" Value="离线">
                                                    <Setter Property="Foreground" Value="Gray"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="当前值" Binding="{Binding CurrentValue}" Width="*"/>
                    <DataGridTextColumn Header="关联设备" Binding="{Binding Device}" Width="1.5*"/>
                    <DataGridTextColumn Header="最后更新时间" Binding="{Binding LastUpdatedTime}" Width="1.5*"/>
                    
                    <DataGridTemplateColumn Header="操作" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="详情" Background="#00BFFF" Foreground="White" BorderThickness="0" Padding="10,4" Margin="0,0,5,0" Cursor="Hand">
                                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="2"/></Style></Button.Resources>
                                    </Button>
                                    <Button Content="模拟" Background="#1F3A60" Foreground="White" BorderThickness="0" Padding="10,4" Cursor="Hand">
                                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="2"/></Style></Button.Resources>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- 底部分页栏 -->
            <Grid Grid.Row="2" Margin="0,15,0,0">
                <TextBlock Text="共 245 条记录" Foreground="Gray" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <ComboBox SelectedIndex="0" Width="80" Margin="0,0,15,0">
                        <ComboBoxItem Content="10条/页"/>
                        <ComboBoxItem Content="20条/页"/>
                    </ComboBox>
                    
                    <Button Content="&lt;" Background="Transparent" Foreground="Gray" BorderBrush="#1F3A60" BorderThickness="1" Width="30" Height="30" Margin="0,0,5,0"/>
                    <Button Content="1" Background="#00BFFF" Foreground="White" BorderThickness="0" Width="30" Height="30" Margin="0,0,5,0"/>
                    <Button Content="2" Background="Transparent" Foreground="White" BorderBrush="#1F3A60" BorderThickness="1" Width="30" Height="30" Margin="0,0,5,0"/>
                    <Button Content="3" Background="Transparent" Foreground="White" BorderBrush="#1F3A60" BorderThickness="1" Width="30" Height="30" Margin="0,0,5,0"/>
                    <TextBlock Text="..." Foreground="Gray" VerticalAlignment="Center" Margin="5,0,10,0"/>
                    <Button Content="25" Background="Transparent" Foreground="White" BorderBrush="#1F3A60" BorderThickness="1" Width="30" Height="30" Margin="0,0,5,0"/>
                    <Button Content="&gt;" Background="Transparent" Foreground="Gray" BorderBrush="#1F3A60" BorderThickness="1" Width="30" Height="30"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
