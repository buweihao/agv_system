<UserControl x:Class="AgvDispatcher.Modules.SignalModule.Views.SignalLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="Transparent">

    <UserControl.Resources>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#112233"/>
            <Setter Property="Foreground" Value="#00BFFF"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#1F3A60"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#1F3A60"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2A3B4C"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="#0F1A2A" BorderBrush="#1F3A60" BorderThickness="1" CornerRadius="6" Padding="15">
        <Grid>
            <DataGrid ItemsSource="{Binding LogList}" AutoGenerateColumns="False" Background="Transparent" 
                      RowBackground="Transparent" AlternatingRowBackground="#0A111E" 
                      Foreground="White" GridLinesVisibility="None" BorderThickness="0" HeadersVisibility="Column"
                      CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="时间" Binding="{Binding LogTime}" Width="*"/>
                    <DataGridTextColumn Header="信号名称" Binding="{Binding SignalName}" Width="1.5*"/>
                    <DataGridTextColumn Header="信号ID" Binding="{Binding SignalId}" Width="1.5*"/>
                    <DataGridTextColumn Header="信号类型" Binding="{Binding SignalType}" Width="*"/>
                    
                    <!-- 状态变化特化 -->
                    <DataGridTemplateColumn Header="状态变化" Width="1.5*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding StateChange}" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="White"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding StateChange}" Value="正常 -> 异常">
                                                    <Setter Property="Foreground" Value="#FF4500"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding StateChange}" Value="正常 -> 激活">
                                                    <Setter Property="Foreground" Value="#FFD700"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding StateChange}" Value="激活 -> 正常">
                                                    <Setter Property="Foreground" Value="#00FF7F"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding StateChange}" Value="异常 -> 正常">
                                                    <Setter Property="Foreground" Value="#00FF7F"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding StateChange}" Value="正常 -> 离线">
                                                    <Setter Property="Foreground" Value="Gray"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <DataGridTextColumn Header="触发值" Binding="{Binding TriggerValue}" Width="*"/>
                    <DataGridTextColumn Header="关联设备" Binding="{Binding Device}" Width="*"/>
                    
                    <!-- 响应时间 右对齐 -->
                    <DataGridTemplateColumn Header="响应时间" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ResponseTime}" HorizontalAlignment="Right" Foreground="#00BFFF"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- 结果特化 -->
                    <DataGridTemplateColumn Header="结果" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="2" Padding="8,2" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding Result}" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Result}" Value="成功">
                                                    <Setter Property="Background" Value="#2000FF7F"/>
                                                    <Setter Property="BorderBrush" Value="#00FF7F"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                    <Setter Property="TextBlock.Foreground" Value="#00FF7F"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Result}" Value="失败">
                                                    <Setter Property="Background" Value="#20FF4500"/>
                                                    <Setter Property="BorderBrush" Value="#FF4500"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                    <Setter Property="TextBlock.Foreground" Value="#FF4500"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>
