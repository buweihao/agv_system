<UserControl x:Class="AgvDispatcher.Modules.SystemConfigModule.Views.ChargeStationConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E6F4FF"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
        </Style>
        <Style x:Key="PanelBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#0D1724"/>
            <Setter Property="BorderBrush" Value="#1F3A60"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="14"/>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="MinWidth" Value="88"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Background" Value="#12304A"/>
            <Setter Property="Foreground" Value="#DCEFFF"/>
            <Setter Property="BorderBrush" Value="#2D5D88"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="DataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#0B1828"/>
            <Setter Property="Foreground" Value="#DCEFFF"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#1F3A60"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#0A111E">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource PanelBorderStyle}" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="充电桩配置" Foreground="#00BFFF" FontSize="18" FontWeight="Bold"/>
                    <TextBlock Text="管理充电桩节点和状态" Foreground="#8EA8C3" FontSize="12" Margin="0,6,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding TotalStations, StringFormat=充电桩总数 {0}}" Foreground="#8EA8C3" VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <Button Content="刷新" Command="{Binding RefreshCommand}" Style="{StaticResource ActionButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource PanelBorderStyle}">
            <DockPanel>
                <Grid DockPanel.Dock="Top" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="充电桩列表" Foreground="#00BFFF" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="新增电桩" Command="{Binding AddStationCommand}" Style="{StaticResource ActionButtonStyle}"/>
                        <Button Content="删除选中" Command="{Binding DeleteStationCommand}" Background="#FF4500" Foreground="White" BorderThickness="0" Style="{StaticResource ActionButtonStyle}"/>
                        <Button Content="保存选中" Command="{Binding SaveStationCommand}" Style="{StaticResource ActionButtonStyle}" Background="#00BFFF" Foreground="#04101C" BorderThickness="0" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
                <DataGrid ItemsSource="{Binding Stations}"
                          SelectedItem="{Binding SelectedStation, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          IsReadOnly="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          RowHeight="36"
                          Background="Transparent"
                          BorderThickness="0"
                          RowBackground="Transparent"
                          AlternatingRowBackground="#101E30"
                          Foreground="#DCEFFF"
                          ColumnHeaderStyle="{StaticResource DataGridHeaderStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="充电桩ID" Binding="{Binding StationId}" Width="*"/>
                        <DataGridTextColumn Header="充电桩名称" Binding="{Binding Name}" Width="*"/>
                        <DataGridTemplateColumn Header="绑定节点ID" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding NodeId}" VerticalAlignment="Center" Margin="5,0"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{Binding DataContext.AvailableNodeIds, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                              SelectedItem="{Binding NodeId, UpdateSourceTrigger=PropertyChanged}" 
                                              IsEditable="True"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="所属区域" Binding="{Binding AreaCode}" Width="*"/>
                        <DataGridTextColumn Header="支持品牌" Binding="{Binding AllowedBrands}" Width="*"/>
                        <DataGridTextColumn Header="协议" Binding="{Binding ProtocolType}" Width="*"/>
                        <DataGridTextColumn Header="地址" Binding="{Binding Endpoint}" Width="*"/>
                        <DataGridTextColumn Header="端口" Binding="{Binding Port}" Width="0.6*"/>
                        <DataGridTextColumn Header="额定功率" Binding="{Binding RatedPowerKw, StringFormat='\{0:F1\} kW'}" Width="*"/>
                        <DataGridTextColumn Header="电压" Binding="{Binding OutputVoltage, StringFormat='\{0:F1\} V'}" Width="0.8*"/>
                        <DataGridTextColumn Header="电流" Binding="{Binding OutputCurrent, StringFormat='\{0:F1\} A'}" Width="0.8*"/>
                        <DataGridCheckBoxColumn Header="是否启用" Binding="{Binding IsEnabled}" Width="0.8*"/>
                        <DataGridTextColumn Header="接口类型" Binding="{Binding ConnectorType}" Width="*"/>
                        <DataGridTextColumn Header="支持车型" Binding="{Binding SupportedVehicleTypes}" Width="*"/>
                        <DataGridCheckBoxColumn Header="自动充电" Binding="{Binding SupportAutoCharge}" Width="0.8*"/>
                        <DataGridTextColumn Header="最大排队" Binding="{Binding MaxQueueCount}" Width="0.8*"/>
                        <DataGridCheckBoxColumn Header="独占模式" Binding="{Binding IsExclusive}" Width="0.8*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
