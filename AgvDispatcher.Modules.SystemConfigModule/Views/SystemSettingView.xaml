<UserControl x:Class="AgvDispatcher.Modules.SystemConfigModule.Views.SystemSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:local="clr-namespace:AgvDispatcher.Modules.SystemConfigModule.Views"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <Style x:Key="ConfigNavStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#8EA8C3"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="Margin" Value="8,3"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{TemplateBinding Tag}" FontFamily="Segoe MDL2 Assets" Foreground="{TemplateBinding Foreground}" FontSize="15" VerticalAlignment="Center"/>
                                <ContentPresenter Grid.Column="1" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#112233"/>
                                <Setter Property="Foreground" Value="#DCEFFF"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#16314A"/>
                                <Setter Property="Foreground" Value="#00BFFF"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#0A111E">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="230"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Margin="10" Background="#0D1724" BorderBrush="#1F3A60" BorderThickness="1" CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Margin="16,18,16,14">
                    <TextBlock Text="系统配置" Foreground="#00BFFF" FontSize="18" FontWeight="Bold"/>
                    <TextBlock Text="设备、地图、规则和运行参数" Foreground="#7893AA" FontSize="12" Margin="0,6,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1">
                    <RadioButton x:Name="RbVehicleConfig" Tag="&#xE950;" Content="车辆配置" Style="{StaticResource ConfigNavStyle}" IsChecked="True"/>
                    <RadioButton Tag="&#xE81E;" Content="地图配置" Style="{StaticResource ConfigNavStyle}" IsEnabled="False"/>
                    <RadioButton Tag="&#xE945;" Content="充电桩配置" Style="{StaticResource ConfigNavStyle}" IsEnabled="False"/>
                    <RadioButton Tag="&#xE713;" Content="系统参数" Style="{StaticResource ConfigNavStyle}" IsEnabled="False"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Margin="0,10,10,10">
            <local:VehicleConfigView>
                <local:VehicleConfigView.Style>
                    <Style TargetType="UserControl">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsChecked, ElementName=RbVehicleConfig}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </local:VehicleConfigView.Style>
            </local:VehicleConfigView>
        </Grid>
    </Grid>
</UserControl>
