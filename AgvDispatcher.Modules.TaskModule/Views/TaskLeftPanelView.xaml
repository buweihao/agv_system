<UserControl x:Class="AgvDispatcher.Modules.TaskModule.Views.TaskLeftPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="320"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="#0A111E">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
        </Style>
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#00BFFF"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#0F1A2A"/>
            <Setter Property="BorderBrush" Value="#1F3A60"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 1. 任务概览 -->
        <Border Grid.Row="0" Style="{StaticResource CardBorderStyle}">
            <StackPanel>
                <TextBlock Text="▶ 任务概览" Style="{StaticResource SectionTitleStyle}"/>
                <UniformGrid Columns="3" Rows="2" VerticalAlignment="Top">
                    <StackPanel Margin="5">
                        <TextBlock Text="任务总数" Foreground="Gray" FontSize="12"/>
                        <TextBlock Text="{Binding TotalTasks}" Foreground="#00BFFF" FontSize="20" FontWeight="Bold"/>
                    </StackPanel>
                    <StackPanel Margin="5">
                        <TextBlock Text="运行中" Foreground="Gray" FontSize="12"/>
                        <TextBlock Text="{Binding RunningTasks}" Foreground="#00FF7F" FontSize="20" FontWeight="Bold"/>
                    </StackPanel>
                    <StackPanel Margin="5">
                        <TextBlock Text="待执行" Foreground="Gray" FontSize="12"/>
                        <TextBlock Text="{Binding PendingTasks}" Foreground="#FFD700" FontSize="20" FontWeight="Bold"/>
                    </StackPanel>
                    <StackPanel Margin="5">
                        <TextBlock Text="已完成" Foreground="Gray" FontSize="12"/>
                        <TextBlock Text="{Binding CompletedTasks}" Foreground="#00FF7F" FontSize="20" FontWeight="Bold"/>
                    </StackPanel>
                    <StackPanel Margin="5">
                        <TextBlock Text="失败" Foreground="Gray" FontSize="12"/>
                        <TextBlock Text="{Binding FailedTasks}" Foreground="#FF4500" FontSize="20" FontWeight="Bold"/>
                    </StackPanel>
                    <StackPanel Margin="5">
                        <TextBlock Text="成功率" Foreground="Gray" FontSize="12"/>
                        <TextBlock Text="{Binding SuccessRateText}" Foreground="#00BFFF" FontSize="20" FontWeight="Bold"/>
                    </StackPanel>
                </UniformGrid>
            </StackPanel>
        </Border>

        <!-- 2. 任务状态占比 -->
        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
            <StackPanel>
                <TextBlock Text="▶ 任务状态占比" Style="{StaticResource SectionTitleStyle}"/>
                <Grid Height="120">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Grid Grid.Column="0" Width="120">
                        <Ellipse Width="100" Height="100" Stroke="#1F3A60" StrokeThickness="10" />
                        <Ellipse Width="100" Height="100" Stroke="#00BFFF" StrokeThickness="10" StrokeDashArray="30 4" />
                        <Viewbox StretchDirection="DownOnly" MaxHeight="40" Margin="15,0">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="{Binding TotalTasks}" FontSize="22" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                <TextBlock Text="总数" FontSize="12" Foreground="Gray" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Viewbox>
                    </Grid>

                    <Viewbox Grid.Column="1" Margin="15,0,0,0" StretchDirection="DownOnly" HorizontalAlignment="Left">
                        <StackPanel VerticalAlignment="Center" MinWidth="100">
                            <Grid Margin="0,3">
                                <TextBlock Text="运行中" Foreground="White" FontSize="12" HorizontalAlignment="Left"/>
                                <TextBlock Text="{Binding RunningPercentText}" Foreground="#00FF7F" FontSize="12" HorizontalAlignment="Right" FontWeight="Bold"/>
                            </Grid>
                            <Grid Margin="0,3">
                                <TextBlock Text="待执行" Foreground="White" FontSize="12" HorizontalAlignment="Left"/>
                                <TextBlock Text="{Binding PendingPercentText}" Foreground="#FFD700" FontSize="12" HorizontalAlignment="Right" FontWeight="Bold"/>
                            </Grid>
                            <Grid Margin="0,3">
                                <TextBlock Text="已完成" Foreground="White" FontSize="12" HorizontalAlignment="Left"/>
                                <TextBlock Text="{Binding CompletedPercentText}" Foreground="#00BFFF" FontSize="12" HorizontalAlignment="Right" FontWeight="Bold"/>
                            </Grid>
                            <Grid Margin="0,3">
                                <TextBlock Text="失败" Foreground="White" FontSize="12" HorizontalAlignment="Left"/>
                                <TextBlock Text="{Binding FailedPercentText}" Foreground="#FF4500" FontSize="12" HorizontalAlignment="Right" FontWeight="Bold"/>
                            </Grid>
                        </StackPanel>
                    </Viewbox>
                </Grid>
            </StackPanel>
        </Border>

        <!-- 3. 任务趋势(近24小时) -->
        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}">
            <StackPanel>
                <TextBlock Text="▶ 任务趋势 (近24小时)" Style="{StaticResource SectionTitleStyle}"/>
                <Viewbox StretchDirection="DownOnly" HorizontalAlignment="Left">
                    <Grid Height="120" Width="260">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="20"/>
                        </Grid.RowDefinitions>
                        
                        <Canvas Grid.Row="0" Margin="0,5,0,0" ClipToBounds="True">
                            <!-- Background Grid Lines -->
                            <Line X1="0" Y1="25" X2="260" Y2="25" Stroke="#1F3A60" StrokeThickness="1" StrokeDashArray="2 2"/>
                            <Line X1="0" Y1="50" X2="260" Y2="50" Stroke="#1F3A60" StrokeThickness="1" StrokeDashArray="2 2"/>
                            <Line X1="0" Y1="75" X2="260" Y2="75" Stroke="#1F3A60" StrokeThickness="1" StrokeDashArray="2 2"/>
                            
                            <!-- Created Tasks -->
                            <Polyline Points="0,80 40,60 80,70 120,40 160,50 200,20 240,40 260,10" Stroke="#00BFFF" StrokeThickness="2" />
                            
                            <!-- Completed Tasks -->
                            <Polyline Points="0,90 40,75 80,85 120,60 160,70 200,45 240,55 260,30" Stroke="#00FF7F" StrokeThickness="2" />
                            
                            <StackPanel Orientation="Horizontal" Canvas.Right="0" Canvas.Top="-5">
                                <Ellipse Width="8" Height="8" Fill="#00BFFF" Margin="0,0,3,0"/><TextBlock Text="创建" Foreground="Gray" FontSize="10" Margin="0,0,10,0"/>
                                <Ellipse Width="8" Height="8" Fill="#00FF7F" Margin="0,0,3,0"/><TextBlock Text="完成" Foreground="Gray" FontSize="10"/>
                            </StackPanel>
                        </Canvas>

                        <!-- X-Axis Labels -->
                        <Grid Grid.Row="1">
                            <TextBlock Text="00:00" Foreground="Gray" FontSize="10" HorizontalAlignment="Left"/>
                            <TextBlock Text="06:00" Foreground="Gray" FontSize="10" HorizontalAlignment="Center" Margin="-65,0,0,0"/>
                            <TextBlock Text="12:00" Foreground="Gray" FontSize="10" HorizontalAlignment="Center" Margin="65,0,0,0"/>
                            <TextBlock Text="18:00" Foreground="Gray" FontSize="10" HorizontalAlignment="Right"/>
                        </Grid>
                    </Grid>
                </Viewbox>
            </StackPanel>
        </Border>

        <!-- 4. 任务类型分布 -->
        <Border Grid.Row="3" Style="{StaticResource CardBorderStyle}" Margin="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="▶ 任务类型分布" Style="{StaticResource SectionTitleStyle}"/>
                
                <Viewbox Grid.Row="1" StretchDirection="DownOnly" VerticalAlignment="Top" HorizontalAlignment="Left">
                    <StackPanel Width="260">
                        <Grid Margin="0,0,0,12">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" Margin="0,0,0,5">
                                <TextBlock Text="搬运任务" Foreground="White" FontSize="12" HorizontalAlignment="Left"/>
                                <TextBlock Text="84 (50%)" Foreground="White" FontSize="12" HorizontalAlignment="Right"/>
                            </Grid>
                            <ProgressBar Grid.Row="1" Minimum="0" Maximum="100" Value="50" Height="16" Background="#1F3A60" Foreground="#00BFFF" BorderThickness="0"/>
                        </Grid>

                        <Grid Margin="0,0,0,12">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" Margin="0,0,0,5">
                                <TextBlock Text="补货任务" Foreground="White" FontSize="12" HorizontalAlignment="Left"/>
                                <TextBlock Text="42 (25%)" Foreground="White" FontSize="12" HorizontalAlignment="Right"/>
                            </Grid>
                            <ProgressBar Grid.Row="1" Minimum="0" Maximum="100" Value="25" Height="16" Background="#1F3A60" Foreground="#00FF7F" BorderThickness="0"/>
                        </Grid>

                        <Grid Margin="0,0,0,12">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" Margin="0,0,0,5">
                                <TextBlock Text="巡检任务" Foreground="White" FontSize="12" HorizontalAlignment="Left"/>
                                <TextBlock Text="25 (15%)" Foreground="White" FontSize="12" HorizontalAlignment="Right"/>
                            </Grid>
                            <ProgressBar Grid.Row="1" Minimum="0" Maximum="100" Value="15" Height="16" Background="#1F3A60" Foreground="#FFD700" BorderThickness="0"/>
                        </Grid>
                        
                        <Grid Margin="0,0,0,12">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" Margin="0,0,0,5">
                                <TextBlock Text="充电任务" Foreground="White" FontSize="12" HorizontalAlignment="Left"/>
                                <TextBlock Text="12 (7%)" Foreground="White" FontSize="12" HorizontalAlignment="Right"/>
                            </Grid>
                            <ProgressBar Grid.Row="1" Minimum="0" Maximum="100" Value="7" Height="16" Background="#1F3A60" Foreground="#FF8C00" BorderThickness="0"/>
                        </Grid>
                        
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" Margin="0,0,0,5">
                                <TextBlock Text="其他任务" Foreground="White" FontSize="12" HorizontalAlignment="Left"/>
                                <TextBlock Text="5 (3%)" Foreground="White" FontSize="12" HorizontalAlignment="Right"/>
                            </Grid>
                            <ProgressBar Grid.Row="1" Minimum="0" Maximum="100" Value="3" Height="16" Background="#1F3A60" Foreground="#9370DB" BorderThickness="0"/>
                        </Grid>
                    </StackPanel>
                </Viewbox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
